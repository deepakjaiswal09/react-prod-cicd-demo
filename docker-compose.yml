version: '3.9'

services:
  react-prod-app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: react-prod-container

    ports:
      - "3000:8080"

    restart: unless-stopped
